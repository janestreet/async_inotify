(library
 ((name async_inotify)
  (public_name async_inotify)
  (libraries (async
              async_find
              core
              inotify))
  (preprocess (pps (ppx_jane)))))

(jbuild_version jane_street)
